[pytest]
testpaths = tests
# The default run collects only the GPU-free unit and fake-runner tests.
addopts = -m "not smoke" --strict-markers
markers =
    smoke: real CryoZeta run; needs a GPU, pixi, weights and TEASER++
filterwarnings =
    ignore::DeprecationWarning
    ignore::RuntimeWarning
